enum RouteStatus {
  init,
  error,
  online,
  offline,
  loggedIn,
  notLoggedIn,
  noNetwork,
  serverOffline,
  serverMaintenance
}

enum BubbleType {
  sendBubble,
  receiverBubble,
}

enum ReportType {
  user,
  post,
  comment,
}
